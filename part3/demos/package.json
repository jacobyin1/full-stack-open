{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Jacob Yin",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "Remove-Item -Recurse -Force dist; npm --prefix ../../part2/demos run build; Copy-Item -Recurse ../../part2/demos/dist dist",
    "deploy": "npm run build:ui; git add .; git commit -m uibuild; git push"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
